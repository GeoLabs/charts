{{ if .Values.stageInOut.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: semaphore-argo-cwl-runner-stage-in-out
data:
  workflow: "{{ .Values.stageInOut.semaphore.workflow | default "3" }}"
{{ end }}
